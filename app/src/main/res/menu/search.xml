<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:title="search"
        android:id="@+id/search_option"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="ifRoom"
        app:actionViewClass="androidx.appcompat.widget.SearchView"/>

    <item
        android:title="Sort by"
        android:id="@+id/sort_options"
        app:showAsAction="never">
        <menu>
            <item
                android:title="Name (Asc)"
                android:id="@+id/by_name_asc"
                app:showAsAction="never"/>
            <item
                android:title="Name (Desc)"
                android:id="@+id/by_name_desc"
                app:showAsAction="never"/>
            <item
                android:title="Date (Asc)"
                android:id="@+id/by_date_asc"
                app:showAsAction="never"/>
            <item
                android:title="Date (Desc)"
                android:id="@+id/by_date_desc"
                app:showAsAction="never"/>
            <item
                android:title="Size (Asc)"
                android:id="@+id/by_size_asc"
                app:showAsAction="never"/>
            <item
                android:title="Size (Desc)"
                android:id="@+id/by_size_desc"
                app:showAsAction="never"/>
        </menu>
    </item>
</menu>